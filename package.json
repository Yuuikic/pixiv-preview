{
  "name": "p2-pixiv-preview",
  "version": "1.0.0",
  "private": true,
  "description": "P²: A browser extension for previewing Pixiv artworks, designed especially to work with Nazurin.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node scripts/check-release.mjs",
    "package": "node scripts/package-extension.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
